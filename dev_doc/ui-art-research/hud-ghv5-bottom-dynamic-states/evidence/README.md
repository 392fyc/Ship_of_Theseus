# GHV-5 运行时状态证据

状态：三张 1280×720 Godot 截图已切换为默认五技能并通过本次独立审查，等待用户视觉审核。

## 审核文件

- `hud_bottom_dynamic_capacity_1_ready_1280x720.png`：容量 1、正常移动、零护盾。
- `hud_bottom_dynamic_capacity_2_early_lock_1280x720.png`：容量 2、剩余移动灰化、长名称、极大数值、冷却 2 和次数 3。
- `hud_bottom_dynamic_capacity_3_exhausted_1280x720.png`：容量 3、移动灰色 0、冷却清除和次数隐藏。

## 人工检查结论

- 三图都包含完整 20×20 棋盘、中央剑圣比例锚点、五区底栏、行动资源条和结束回合按钮。
- 状态二的移动值 7 被保留但整体灰化；状态三显示灰色 0，两种语义可区分。
- 状态二的青色 runtime mock 色块经过 alpha 0.5 冷却暗层后仍可辨认，数字 2 清楚；状态三清除暗层后同一色块明显恢复亮度。
- 长名称显示为单行省略，完整文字仍由运行时 tooltip 保存；极大数值使用 9px，状态三短值恢复 11px。
- 行动点只显示实际容量和实际消耗；没有未解锁的虚假点。
- `1`、`2`、`Q`、`E`、`R` 五个快捷键位于各自角标内；没有主动／被动文字或可见分类标志。
- 除冷却透明度对照色块外，内容图标均为空。

捕获器只有在场景状态正确、五个技能已重建，并且角色、装备、技能、遗物、结束、行动条和棋子关键区域连续两帧包含足够可见像素后才保存。
