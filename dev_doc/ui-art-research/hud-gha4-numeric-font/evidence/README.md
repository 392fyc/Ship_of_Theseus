# GHA-4 HUD 数值字体候选证据

状态：两张 1280×720 Godot 截图已生成，并通过主任务人工检查与独立审查；等待用户视觉审核。

## 审核文件

- `hud_numeric_font_candidate_1280x720.png`：相同几何下，左侧当前回退字体与右侧 IBM Plex Mono SemiBold 的 1:1 对照。
- `hud_bottom_numeric_font_preview_1280x720.png`：在 GHV-5 长值状态中，仅给 XP、HP、SH 局部应用候选字体的实际 HUD 预览。

## 主任务人工检查

- 右列候选数字的笔画比当前回退字体更稳定、更醒目；`0/6/8/9` 的内部空间和斜杠仍可辨。
- 7px 的五个长值均完整落在 58px 右对齐边界内；没有靠裁切隐藏字符。
- 8px 短值 `34/100` 完整，且与长值共享相同右边界。
- 实景预览中的 XP、HP、SH 三行都保持 `999999/999999`、7px 和原有轨道几何；身份栏、技能、行动资源和其他字体未改变。
- 两张图都是实际 Godot 传统栅格化结果，没有最近邻放大诊断图，也没有图像化文字。

## 非阻断边界

- 该候选尚未写入共享 Theme，也未接入正式战斗场景。
- 用户需要根据原图判断小字号粗细是否符合整体 HUD 质感；技术通过不代替该视觉选择。
- 正式采用后，还需在可交付安装内容中提供用户可读的 OFL 许可文本。
